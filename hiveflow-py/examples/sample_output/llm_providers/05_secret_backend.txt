1. Default backend
   Type: EnvVarBackend
   HOME = C:\Users\egonzalez

2. Dictionary backend
   Type: DictBackend
   OPENAI_API_KEY = sk-test-not-a-real-key
   MISSING_KEY    = None
   Protocol check: passed

3. Vault-style backend (skeleton)
   Type: VaultBackend
   OPENAI_API_KEY         = vault-resolved-openai-key
   AZURE_OPENAI_ENDPOINT  = https://prod.openai.azure.com
   ANTHROPIC_API_KEY      = None

4. Providers use the active backend
   All providers call get_secret_backend().get_secret(key) at
   connection time, so swapping the backend before creating a
   provider client changes where credentials come from.
   Available providers: ['anthropic', 'azure', 'openai']
   (Each would use VaultBackend for its keys if called now)

   Restored default EnvVarBackend.
